# GitHub API Authentication (optional but recommended)
# Get token from: https://github.com/settings/tokens
# Required scopes: public_repo (read-only)
# Increases rate limit from 60 to 5000 requests/hour
GITHUB_TOKEN=

# Bob API Key (PRIMARY analysis engine)
# Get from IBM Bob dashboard or use the provided production key
# Format: bob_prod_bob-apikey_...
BOB_API_KEY=

# Bob API Configuration
BOB_API_URL=https://api.bob.build/v1/chat/completions

# Gemini API Key (FALLBACK analysis engine)
# Get from: https://aistudio.google.com/app/apikey
# Used automatically if Bob API fails (rate limits, timeouts, errors)
# Format: AIza...
GEMINI_API_KEY=

# Next.js Configuration
# Used for generating shareable result URLs
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Analysis Configuration
# Maximum number of files to analyze (prevents hanging on massive repos)
MAX_FILES_TO_ANALYZE=50

# Analysis timeout in milliseconds (ensures completion within user expectation)
ANALYSIS_TIMEOUT_MS=30000